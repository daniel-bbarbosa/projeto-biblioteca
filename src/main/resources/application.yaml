server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:projetobiblio
    username: admin
    password: saw567
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html